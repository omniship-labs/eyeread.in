dist
node_modules
src-tauri/target
src-tauri/gen
design
.playwright-mcp
# Test packs: some are deliberately malformed or minified.
spec/packs/fixtures
# Signed byte-for-byte (see src-tauri/src/packs/signature.rs); reformatting breaks the signature.
src-tauri/src/packs/revoked.json
