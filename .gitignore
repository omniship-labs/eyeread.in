node_modules
.playwright-mcp
dist
src-tauri/target
src-tauri/gen
.DS_Store
*.local

# Sponsor avatars are downloaded at build time by scripts/fetch-sponsors.mjs.
# src/data/sponsors.json IS committed so the app import always resolves.
public/sponsors/
