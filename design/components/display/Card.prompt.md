Surface container. `default`/`raised` for app chrome; `glass` is the translucent overlay panel (see-through, blurred); `accent` adds the indigo glow ring.

```jsx
<Card>Script details…</Card>
<Card variant="glass" padding="lg">Floating prompter panel</Card>
<Card variant="accent" interactive>Featured script</Card>
```
